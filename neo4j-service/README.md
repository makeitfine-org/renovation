### Neo4j service for working with neo4j database ###

### Build and run test
`$>  c neo4j-service/ && mci && c .. && dcdw && d rmi koresmosto/renovation-neo4j-service:latest && docker compose -f docker-compose.yml -f docker-compose-debug.yml up renovation-neo4j-service`  
