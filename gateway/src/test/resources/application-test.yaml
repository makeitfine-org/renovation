# and import the 'main' properties
spring:
  security:
    credentials:
      username: all-test
      password: test

    oauth2:
      client:
        provider:
          oauth-client:
            token-uri: http://${KEYCLOAK_HOSTNAME:localhost}:${KEYCLOAK_PORT:18080}/realms/${KEYCLOAK_REALM:renovation-realm}/protocol/openid-connect/token
