global:
  parentChartName: renovation

image:
  repository: koresmosto/renovation-backend
  tag: latest
  pullPolicy: IfNotPresent

postgres:
#  host: postgres
#  port: 5432
  db: renovation
  schema: backend
  user: postgres # todo: secret
  password: postgres1 # todo: secret
  service:
    type: NodePort
    nodePort: 30432

redis:
  host: redis
  port: 6379
  password: redispass
  service:
    type: NodePort
    nodePort: 30379

backend:
  type: NodePort
  port: 8080
  nodePort: 30080
