global:
  secrets: # define whether to create secrets and which namespaces to use
    enabled: false
    vault:
      servicePath: "http://vaultrs.security.svc.cluster.local:8200"
    namespaces:
      - db
      - apps

  # !todo: old (review wether to leave)
  parentChartName: renovation
  namespace:
    infra: infra       # ingress, service-mesh control plane, monitoring, cert-manager, logging
    db: db             # databases, message brokers, caches
    apps: apps         # application workloads
    security: security # Keycloak, Vault, etc.

# dep. guid: https://developer.hashicorp.com/vault/tutorials/kubernetes/kubernetes-raft-deployment-guide
externalc:
  enabled: false
  external-secrets:
    name: extsecrets
    namespaceOverride: security
    installCRDs: true

vaultc:
  enabled: false
  vault:
    secretPath: "secret/renovation/secrets"
    ui:
      enabled: true
      serviceType: NodePort
      serviceNodePort: 30820
    server:
      dev:
        enabled: false
      dataStorage:
        enabled: true
        size: 1Gi
        storageClass: vault-retain

postgresqlc: #bitnami
  enabled: false
  namespace: db
  configMapNamespaces: # create configMap based on the service and propagate it to namespaces
    - apps
  postgresql: #bitnami
    architecture: replication
    fullnameOverride: postgresql
    auth:
      username:
      password:
      database:
      replicationUsername: # !!! todo: create separate rep_user
      replicationPassword: # !!! todo: create separate rep_pass
    schema:
    primary:
      service:
        type: NodePort
        nodePorts:
          postgresql: 30432
      initdb:
        scriptsConfigMap: postgresql-init-configmap
      persistence:
        existingClaim: postgres-primary-0-pvc # !!! should be created in k8s
    readReplicas:
      replicaCount: 1
      service:
        type: NodePort
        nodePorts:
          postgresql: 30433
      persistence:
        existingClaim: postgres-replica-0-pvc # !!! should be created in k8s

redisc:
  enabled: false
  namespace: db
  configMapNamespaces:
    - apps
  redis: #bitnami
    architecture: replication
    fullnameOverride: redis
    auth:
      enabled: true
      password:
    master:
      service:
        type: NodePort
        nodePorts:
          redis: "30379"
      persistence:
        existingClaim: redis-master-0-pvc # !!! should be created in k8s
    replica:
      replicaCount: 1
      service:
        type: NodePort
        nodePorts:
          redis: "30380"
      persistence:
        existingClaim: redis-replica-0-pvc # !!! should be created in k8s

mongodbc:
  enabled: false
  namespace: db
  configMapNamespaces:
    - apps
  mongodb: #bitnami
    architecture: standalone
    global:
      namespaceOverride: db # only this rewrites namespace
    fullnameOverride: mongodb
    auth:
      rootUser:
      rootPassword:
      databases:
        -
      usernames:
        -
      passwords:
        -
    service:
      type: NodePort
      nodePorts:
        mongodb: 30017
    persistence:
      existingClaim: mongodb-master-0-pvc # !!! should be created in k8s
    initdbScriptsConfigMap: mongo-init-configmap

info:
  enabled: false
  name: info
  replicas: 2
  image:
    repository: koresmosto/renovation-info
    tag: latest
    pullPolicy: IfNotPresent
  mongo:
    username: $MONGO_USERNAME
    password: $MONGO_PASSWORD
  service:
    type: NodePort
    port: 9090
    nodePort: 30090
  healthCheck:
    livenessProbe:
      http:
        path: /actuator/health/liveness
      initialDelaySeconds: 30
      periodSeconds: 10
      timeoutSeconds: 5
      failureThreshold: 3
    readinessProbe:
      http:
        path: /actuator/health/readiness
      initialDelaySeconds: 10
      periodSeconds: 5
      timeoutSeconds: 3
      failureThreshold: 3
  resources:
    limits:
      cpu: "1000m"
      memory: "1Gi"
    requests:
      cpu: "500m"
      memory: "512Mi"

backend:
  enabled: false
  namespaceOverride: apps
  name: backend
  replicas: 2
  image:
    repository: koresmosto/renovation-backend
    tag: latest
    pullPolicy: IfNotPresent
  service:
    type: NodePort
    port: 8080
    nodePort: 30080
  healthCheck:
    livenessProbe:
      http:
        path: /actuator/health/liveness
      initialDelaySeconds: 30
      periodSeconds: 10
      timeoutSeconds: 5
      failureThreshold: 3
    readinessProbe:
      http:
        path: /actuator/health/readiness
      initialDelaySeconds: 10
      periodSeconds: 5
      timeoutSeconds: 3
      failureThreshold: 3
  resources:
    limits:
      cpu: "1000m"
      memory: "1Gi"
    requests:
      cpu: "500m"
      memory: "512Mi"
