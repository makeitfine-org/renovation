{{- $ctx := dict "Values" .Values "Release" .Release "Chart" .Chart "containerPort" 8080 }}
{{ include "ca.deployment" $ctx }}
          volumeMounts:
            - name: initdb-scripts
              mountPath: /docker-entrypoint-initdb.d
              readOnly: true

      volumes:
        - name: initdb-scripts
          configMap:
            name: postgres-initdb-scripts
