apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-secret
type: Opaque
data:
  SPRING_DATASOURCE_USERNAME: {{ .Values.postgres.user | b64enc }}
  SPRING_DATASOURCE_PASSWORD: {{ .Values.postgres.password | b64enc }}
