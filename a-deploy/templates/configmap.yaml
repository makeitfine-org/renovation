{{- $root := . }}

{{- $nsList := list }}
{{- range $key, $ns := $root.Values.global.namespace }}
  {{- $nsList = append $nsList $ns }}
{{- end }}
{{- $nsList = append $nsList $root.Release.Namespace }}

{{- range $ns := $nsList }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $root.Values.global.parentChartName }}-configmap
  namespace: {{ $ns }}
data:
  POSTGRES_DB: {{ $root.Values.postgres.db | quote }}

  POSTGRES_DB_URL: "jdbc:postgresql://{{ $root.Values.postgres.name }}.{{ $root.Values.postgres.namespaceOverride | default $root.Release.Namespace }}.svc.cluster.local:{{ $root.Values.postgres.service.port }}/renovation?currentSchema=backend"

  INFO_SERVICE_URL: "http://{{ $root.Values.info.name }}.{{ $root.Values.info.namespaceOverride | default $root.Release.Namespace }}.svc.cluster.local:{{ $root.Values.info.service.port }}/graphql"

  REDIS_HOST: "{{ $root.Values.redis.name }}.{{ $root.Values.redis.namespaceOverride | default $root.Release.Namespace }}.svc.cluster.local"
  REDIS_PORT: {{ $root.Values.redis.service.port | quote }}

  MONGO_INITDB_DATABASE: {{ $root.Values.mongo.initdb.database | quote }}
  MONGO_INITDB_ROOT_USERNAME: {{ $root.Values.mongo.initdb.rootuser.username | quote }}

  MONGO_USERNAME: {{ $root.Values.info.mongo.username | quote }}
  MONGO_DB: {{ $root.Values.mongo.initdb.database | quote }}
  MONGO_HOST: "{{ $root.Values.mongo.name }}.{{ $root.Values.mongo.namespaceOverride | default $root.Release.Namespace }}.svc.cluster.local"
  MONGO_PORT: {{ $root.Values.mongo.service.port | quote }}
---
{{- end }}
